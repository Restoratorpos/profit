/** The settings vertical's public surface — the gym's own row and this device's preferences. */
export { gymSettingsQuery, useGymSettings } from "./api";
export { SettingsPage } from "./components/settings-page";
export type { GymSettings } from "./types";
